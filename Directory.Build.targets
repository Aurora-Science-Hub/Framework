<Project>

  <!--
    RootNamespace must be evaluated after the project file, so that it picks up
    the AssemblyName each project defines (e.g. AuroraScienceHub.Framework.X).
  -->
  <PropertyGroup>
    <RootNamespace>$(AssemblyName)</RootNamespace>
  </PropertyGroup>

</Project>
